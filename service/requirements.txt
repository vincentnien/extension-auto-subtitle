fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
yt-dlp>=2024.10.22

# STT 引擎（擇一即可；/health 會回報可用狀態）
# Mac GPU:  pip install mlx-whisper
# CPU 通用: pip install faster-whisper
# 雲端免裝: 設定 GROQ_API_KEY 環境變數

# MCP 皮（可選）：
# pip install "mcp"